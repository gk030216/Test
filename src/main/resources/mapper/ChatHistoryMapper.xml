<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.mapper.ChatHistoryMapper">

    <resultMap id="BaseResultMap" type="java.util.HashMap">
        <id column="id" property="id" javaType="java.lang.Integer"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="session_id" property="sessionId"/>
        <result column="user_question" property="userQuestion"/>
        <result column="answer_source" property="answerSource"/>
        <result column="ai_answer" property="aiAnswer"/>
        <result column="knowledge_id" property="knowledgeId"/>
        <result column="rating" property="rating"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 获取用户对话历史（用户端） -->
    <select id="getByUserId" resultType="java.util.HashMap">
        SELECT
            id,
            user_id as userId,
            user_question as userQuestion,
            answer_source as answerSource,
            ai_answer as aiAnswer,
            rating,
            DATE_FORMAT(create_time, '%Y-%m-%d %H:%i:%s') as createTime
        FROM chat_history
        WHERE user_id = #{userId}
        ORDER BY create_time ASC
            LIMIT #{offset}, #{limit}
    </select>

    <!-- 插入对话记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_history (user_id, session_id, user_question, answer_source, ai_answer, knowledge_id)
        VALUES (#{userId}, #{sessionId}, #{userQuestion}, #{answerSource}, #{aiAnswer}, #{knowledgeId})
    </insert>

    <!-- 获取对话总数（用户端） -->
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM chat_history WHERE user_id = #{userId}
    </select>

    <!-- 更新评价 -->
    <update id="updateRating">
        UPDATE chat_history SET rating = #{rating} WHERE id = #{id}
    </update>

    <!-- 删除用户对话历史 -->
    <delete id="deleteByUserId">
        DELETE FROM chat_history WHERE user_id = #{userId}
    </delete>

    <!-- ============= 后台管理接口 ============= -->

    <select id="getAdminList" resultType="java.util.HashMap">
        SELECT
        ch.id,
        ch.user_id as userId,
        ch.user_question as userQuestion,
        ch.ai_answer as aiAnswer,
        ch.answer_source as answerSource,
        ch.rating,
        ch.create_time as createTime,
        u.username as userName,
        u.avatar as userAvatar
        FROM chat_history ch
        LEFT JOIN user u ON ch.user_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (u.username LIKE CONCAT('%', #{keyword}, '%')
                OR ch.user_question LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="answerSource != null">
                AND ch.answer_source = #{answerSource}
            </if>
            <if test="rating != null">
                AND ch.rating = #{rating}
            </if>
        </where>
        ORDER BY ch.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计总数也需要关联 user 表 -->
    <select id="countAdminList" resultType="int">
        SELECT COUNT(*)
        FROM chat_history ch
        LEFT JOIN user u ON ch.user_id = u.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (u.username LIKE CONCAT('%', #{keyword}, '%')
                OR ch.user_question LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="answerSource != null">
                AND ch.answer_source = #{answerSource}
            </if>
            <if test="rating != null">
                AND ch.rating = #{rating}
            </if>
        </where>
    </select>

    <!-- 删除单条对话记录 -->
    <delete id="deleteById">
        DELETE FROM chat_history WHERE id = #{id}
    </delete>

    <!-- 获取所有有对话记录的用户列表 -->
    <select id="getUserListWithHistory" resultType="java.util.HashMap">
        SELECT
        u.id as userId,
        u.username as userName,
        u.avatar as userAvatar,
        COUNT(ch.id) as dialogCount,
        MAX(ch.create_time) as lastTime,
        (SELECT user_question FROM chat_history
        WHERE user_id = u.id
        ORDER BY create_time DESC LIMIT 1) as lastQuestion
        FROM user u
        INNER JOIN chat_history ch ON u.id = ch.user_id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (u.username LIKE CONCAT('%', #{keyword}, '%')
                OR u.nickname LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        GROUP BY u.id
        ORDER BY lastTime DESC
    </select>


</mapper>