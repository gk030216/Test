<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 开启Spring MVC 注解驱动 -->
    <mvc:annotation-driven/>

    <!-- 扫描Controller包 -->
    <context:component-scan base-package="com.pet.controller"/>

    <!-- 注册 FileUploadUtil 为 Bean -->
    <bean id="fileUploadUtil" class="com.pet.util.FileUploadUtil" />

    <!-- 视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 静态资源处理 - 原有配置 -->
    <mvc:resources mapping="/static/**" location="/static/"/>

    <!-- 关键配置：映射外部图片目录 -->
    <!-- 将 /uploads/** 映射到 D:/pet_uploads/ 目录 -->
    <mvc:resources mapping="/uploads/**" location="file:D:/pet_uploads/"/>

    <!-- 如果还想保留原来的 upload 目录（可选） -->
    <mvc:resources mapping="/upload/**" location="/upload/"/>

    <!-- 文件上传配置 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSize" value="10485760"/>
    </bean>

    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <!-- JWT 拦截器（验证Token） -->
        <mvc:interceptor>
            <mvc:mapping path="/api/**"/>
            <mvc:exclude-mapping path="/api/user/login"/>
            <mvc:exclude-mapping path="/api/user/register"/>
            <mvc:exclude-mapping path="/api/user/check-username"/>
            <mvc:exclude-mapping path="/api/user/check-email"/>
            <mvc:exclude-mapping path="/api/code/register"/>
            <mvc:exclude-mapping path="/api/code/forget"/>
            <mvc:exclude-mapping path="/api/pay/alipay/notify"/>
            <bean class="com.pet.interceptor.JwtInterceptor"/>
        </mvc:interceptor>

        <!-- 用户活动拦截器（更新最后访问时间） -->
        <mvc:interceptor>
            <mvc:mapping path="/api/**"/>
            <mvc:exclude-mapping path="/api/user/login"/>
            <mvc:exclude-mapping path="/api/user/register"/>
            <mvc:exclude-mapping path="/api/user/check-username"/>
            <mvc:exclude-mapping path="/api/user/check-email"/>
            <mvc:exclude-mapping path="/api/code/register"/>
            <mvc:exclude-mapping path="/api/code/forget"/>
            <mvc:exclude-mapping path="/api/pay/alipay/notify"/>
            <bean class="com.pet.interceptor.UserActivityInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>