<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.mapper.SystemSettingMapper">

    <resultMap id="BaseResultMap" type="com.pet.entity.SystemSetting">
        <id column="id" property="id"/>
        <result column="setting_key" property="settingKey"/>
        <result column="setting_value" property="settingValue"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM system_settings ORDER BY id
    </select>

    <select id="findByKey" resultMap="BaseResultMap">
        SELECT * FROM system_settings WHERE setting_key = #{key}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_settings (setting_key, setting_value)
        VALUES (#{settingKey}, #{settingValue})
    </insert>

    <update id="update">
        UPDATE system_settings SET setting_value = #{settingValue}
        WHERE setting_key = #{settingKey}
    </update>

    <insert id="saveOrUpdate">
        INSERT INTO system_settings (setting_key, setting_value)
        VALUES (#{settingKey}, #{settingValue})
            ON DUPLICATE KEY UPDATE setting_value = #{settingValue}
    </insert>

</mapper>